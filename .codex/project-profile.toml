schema_version = 1

[bootstrap]
status = "complete"

[project]
purpose = "Standalone clinic staff workspace foundation"
audience = "Clinic staff; platform staff continue to use Payload Admin"

[auth]
profile = "server-side-supabase-bff"
status = "implemented-preview-validation-pending"

[storage]
mode = "none"

[access]
public_routes = ["/login", "/api/auth/login", "/auth/password/reset", "/api/auth/password/reset", "/auth/callback", "/auth/confirm", "/api/auth/callback", "/api/health", "/robots.txt"]
preview_indexing = "disabled"

[modules]
active = ["storybook", "github-actions", "vercel-preview", "vercel-production"]

[delivery]
vercel_setup_status = "production"
production_enabled = true
production_domain = "clinics.findmydoc.eu"
