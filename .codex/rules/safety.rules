prefix_rule(
  pattern = ["git", "reset", "--hard"],
  decision = "forbidden",
  justification = "Never discard worktree changes."
)

prefix_rule(
  pattern = ["git", "clean"],
  decision = "forbidden",
  justification = "Never delete untracked work without explicit user approval."
)
