# Library Rules

- Keep authentication, access, environment, and storage policy server-safe and framework-light.
- Do not add a persistence abstraction until the project profile selects a storage mode.
- Treat public route changes and environment schema changes as security-sensitive.
