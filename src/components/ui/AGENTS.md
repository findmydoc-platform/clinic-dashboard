# Shared UI Rules

- Keep this directory domain-neutral; never import app routes, features, prototype data, or fixtures.
- Follow `DESIGN.md`, existing shadcn conventions, and the repository's accessibility requirements.
- Keep one public component per file with a controlled, semantically named API.
- Add a direct colocated Storybook story for every public component.
