# Deployment metadata.
VERCEL_ENV=
NEXT_PUBLIC_DEPLOYMENT_ENV=

# Optional override for the temporary first-access guard.
# Local development uses the documented fallback when this remains empty.
DASHBOARD_PASSWORD=
SUPABASE_URL=
EXPECTED_SUPABASE_PROJECT_REF=
SUPABASE_PUBLISHABLE_KEY=
PAYLOAD_API_URL=https://preview.findmydoc.eu
DASHBOARD_ORIGIN=http://localhost:3000
CSRF_SIGNING_SECRET=
